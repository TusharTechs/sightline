reference/
node_modules/
.DS_Store
bug-artifacts/

# pipeline working files
pipeline/fixtures/**/frames/
pipeline/out/*.pcm
pipeline/.venv/

# Build output and generated artefacts. Deliverable clips live in docs/clips.
pipeline/out/
pipeline/fixtures/**/narration.wav
pipeline/fixtures/**/*.mp4
**/__pycache__/
*.pyc
app/buildinfo.json

# Local reference only, not part of the published project
DEMO-PLAN.md

# Private correspondence — local reference only
outreach/

# Submission working drafts — local reference only
submission/

# Local editor settings, not part of the project
.vscode/

# Demo build intermediates. Everything here is rebuilt by tools/build-demo.py
# from the takes, the narration and the music, so none of it belongs in the
# repository -- 72MB of segment .mov and .wav files went in once by accident.
# The narration text is the exception: it is a source, not an output.
build/
!build/demo/narration.json
